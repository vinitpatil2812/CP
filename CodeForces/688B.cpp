#include<bits/stdc++.h>
using namespace std;
#define ll long long int
  
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    string n;
    cin >> n;

    cout << n;
    reverse(n.begin(), n.end());
    cout << n;

    return 0;
}