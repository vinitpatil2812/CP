#include<bits/stdc++.h>
using namespace std;
#define ll long long int
  
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    cin >> n;

    if(n % 4 == 0 || n % 7 == 0 || n % 11 == 0) {
        int fourCount = 0, elevenCount = 0;

        while(n > 0) {
            
        }
    }
    else {
        cout << -1;
    }

    return 0;
}